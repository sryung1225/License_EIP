#include <stdio.h>
int main() {
    int j = 024, k = 24, L = 0x24, hap;

    hap=j+k+L;

    printf("%d, %d, %d, %d", j, k, L, hap);

    // 024 : 8진수 => 8*2+1*4=20
    // 24 : 10진수 => 24
    // 0x24 : 16진수 => 16*2+1*4=36
    // hap=20+24+36=80

    // 결과
    // 20, 24, 36, 80
}